我要修改 hello.pl 這個檔案

請把 hello.pl 改成一個最簡單的 Perl + Tk 跨平台 GUI 程式，功能只有：

- 開一個小視窗
- 視窗標題設為「退出測試」
- 視窗大小約 300x150 像素
- 視窗裡只有一個按鈕，文字寫「關閉並退出」
- 按下按鈕後直接結束整個程式（真的退出，不只是關視窗）
- 程式要使用 strict 和 warnings
- 加上 shebang 行 #!/usr/bin/env perl
- 按鈕要盡量放在視窗中間，看起來舒服一點

如果 hello.pl 原本有其他內容，請全部替換成符合以上需求的程式碼
請直接幫我修改 hello.pl